# Roll Call (팀/구역별 명단 생성기)

엑셀 파일을 업로드하여 팀 및 구역별로 구조화된 명단을 자동으로 생성해주는 Streamlit 웹 애플리케이션입니다.

## 👋 프로젝트 소개

- **목적**: 복잡한 엑셀 명단을 팀/구역별로 깔끔하게 정리하여 텍스트 형태로 변환합니다.
- **주요 기능**:
  - 엑셀 파일(.xlsx) 업로드 및 데이터 자동 인식
  - 팀 선택 시 해당 팀의 구역별 명단 자동 그룹핑
  - 커스텀 머리글(공지사항 등) 추가 가능
  - 원클릭 복사 및 텍스트 파일 다운로드 지원

## 🚀 실행 방법 (로컬)

### 1. 환경 설정

Python 3.11 이상이 설치되어 있어야 합니다.

```bash
# 가상환경 생성 (선택사항)
python -m venv venv
source venv/bin/activate  # Mac/Linux
# venv\Scripts\activate  # Windows

# 의존성 설치
pip install -r requirements.txt
```

### 2. 앱 실행

```bash
streamlit run app.py
```

브라우저가 자동으로 열리며 `http://localhost:8501`로 접속됩니다.

## 💾 데이터 형식

업로드하는 엑셀 파일은 반드시 다음 **5가지 열(Column)**을 포함해야 합니다:

- `팀`: 팀명
- `구역`: 소그룹/구역명
- `고유번호`: 개인 식별 번호
- `이름`: 사용자 이름
- `출결여부`: '출결제외'라고 적혀있으면 명단에 `X` 표시가 됨

> **참고**: `sample_data.xlsx` 파일을 통해 테스트해볼 수 있습니다.

## 🐳 Docker 실행

```bash
# 이미지 빌드
docker build -t roll-call .

# 컨테이너 실행
docker run -p 8080:8080 roll-call
```

`http://localhost:8080`으로 접속 가능합니다.

## ☁️ Google Cloud Run 배포

Google Cloud CLI가 설치되어 있다고 가정합니다.

```bash
# 1. Google Cloud 프로젝트 설정
gcloud config set project [YOUR_PROJECT_ID]

# 2. 소스 코드 빌드 및 배포
gcloud run deploy roll-call --source . --region asia-northeast3 --allow-unauthenticated
```
