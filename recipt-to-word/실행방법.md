# 🚀 CIS 청구 문서 자동화 시스템

## 📁 폴더 구조

```
cis-account/
├── back/              # 백엔드 서버
│   ├── main.py
│   ├── requirements.txt
│   └── .env.example
└── front/             # 프론트엔드
    ├── index.html
    ├── app.js
    └── styles.css
```

## 🔧 설치 및 실행

### 1단계: Gemini API 키 설정

```bash
cd back
cp .env.example .env
```

`.env` 파일을 열어서 API 키 입력:
```
GEMINI_API_KEY=your-api-key-here
```

**API 키 발급**: https://makersuite.google.com/app/apikey

---

### 2단계: 패키지 설치

```bash
cd back
pip install -r requirements.txt
```

**권장: Python 3.11 사용**

---

### 3단계: 서버 실행

```bash
cd back
python main.py
```

서버가 `http://localhost:8000`에서 실행됩니다.

---

### 4단계: 프론트엔드 열기

**방법 1: 직접 열기 (추천)**
- `front/index.html` 파일을 더블클릭하여 브라우저에서 열기

**방법 2: Live Server 사용 (VS Code)**
- VS Code에서 `front/index.html` 우클릭 → "Open with Live Server"

---

## ✨ 주요 기능

### 📋 클립보드 붙여넣기
1. 영수증 이미지를 복사 (캡처 또는 파일에서)
2. 드롭존 영역 클릭
3. **Ctrl+V** 로 붙여넣기
4. 자동 OCR 스캔!

### 📁 파일 업로드
- 드롭존 클릭하여 파일 선택
- 드래그 앤 드롭으로 업로드

### 🤖 Gemini AI
- 영수증 자동 스캔
- 날짜/금액 자동 추출
- 원화만 인식 (유로화 제외)

---

## 🎯 빠른 시작

```bash
# 1. 백엔드 실행
cd back
python main.py

# 2. 새 터미널에서 프론트엔드 열기
# front/index.html 파일을 브라우저에서 열기
```

---

## 💡 팁

- 서버가 실행 중이어야 OCR이 작동합니다
- 클립보드 기능은 https 또는 localhost에서만 작동합니다
- 영수증은 밝고 선명하게 촬영하세요

---

## 🐛 문제 해결

### 서버 연결 실패
→ `back` 폴더에서 `python main.py` 실행 확인

### GEMINI_API_KEY 오류
→ `.env` 파일에 API 키가 올바르게 입력되었는지 확인

### 클립보드 붙여넣기 안 됨
→ 드롭존을 클릭한 후 Ctrl+V 누르기

---

## 📞 지원

궁금한 점이 있으시면 README.md를 참조하세요.

**행복한 자동화 되세요!** 🎉

---

## Deployment (Render)

Build command:
```bash
cd front
npm install
npm run build
cd ..\back
pip install -r requirements.txt
```

Start command:
```bash
cd back
uvicorn main:app --host 0.0.0.0 --port $PORT
```

## Local build + run (single service)

```bash
cd front
npm install
npm run build

cd ..\back
pip install -r requirements.txt
uvicorn main:app --host 0.0.0.0 --port 8000
```
